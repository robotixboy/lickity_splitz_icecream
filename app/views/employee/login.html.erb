<%= form_for Addition.new, url: additions_path do |f| %>
  <div class="field">
    <%= f.label :addition_name, "Addition Name" %>
    <%= f.text_field :addition_name, placeholder: "Enter the addition name" %>
  </div>

  <div class="field">
    <%= f.label :cost, "Cost" %>
    <%= f.number_field :cost, step: 0.01, placeholder: "Enter the cost" %>
  </div>

  <div class="actions">
    <%= f.submit "Add Addition" %>
  </div>
<% end %>

<table>
  <tr>
    <th>Food Addition</th>
    <th>Cost</th>
    <th>Action</th>
  </tr>
  <% if @additions.any? %>
    <% @additions.each do |addition| %>
      <tr>
        <td><%= addition.addition_name %></td>
        <td><%= addition.cost %></td>
        <td>
          <%= form_tag storing_additions_path, method: :post do %>
            <%= hidden_field_tag :your_variable, addition.to_json %>
            <%= submit_tag "Store Variable" %>
          <% end %>
        </td>
      </tr>
    <% end %>
  <% else %>
    <tr>
      <td colspan="3">No additions available.</td>
    </tr>
  <% end %>
</table>

<% if !session[:addition].blank? %>
  <% session[:addition].each do |x| %>
    <% object = JSON.parse(x) %>
    <%= object.inspect %>
    <h4> ITS WORKING: <%= object["id"] %> </h4>
  <% end %>
<% end %>

<%= form_with(model: @food, url: addingNewFood_path, method: :post) do |form| %>
  <%= form.text_field :food_name %>
  <%= form.hidden_field :food_additions, value: session[:addition] %>
  <%= form.hidden_field :food_modifiables, value: "" %>
  <%= form.number_field :inital_cost, step: 0.01 %>
  <%= form.submit "Add Food", class: "btn btn-primary" %>
<% end %>

<% if !@foods.blank? %>
  <% @foods.each do |fooditem| %>
    <%= fooditem.food_name %>

    <% data = eval(fooditem.food_additions) rescue [] %>
    <% additions = data.is_a?(Array) ? data : [data] %>
    <% additions.each do |addition| %>
      <h4>ID: <%= addition['id'] %>, Name: <%= addition['addition_name'] %>, Cost: <%= addition['cost'] %></h4>
    <% end %>
  <% end %>
<% end %>


<h1>Order Database</h1>
<table>
<tr>
    <th>Customer Id</th>
    <th>Order Id</th>
    <th>Food Ids</th>
    <th>Food Mods</th>
    <th>Total Cost</th>
</tr>
<% if !@order.blank? %>
  <% @orders.each do |order| %>
    <tr>
      <th><%= order.customer_id %></th>
      <th><%= order.order_id %></th>
      <th><%= order.list_food_id %></th>
      <th><%= order.list_food_notes %></th>
      <th><%= order.total_cost %></th>
      <th><%= order.completed_order %></th>
    </tr>
  <% end %>
<% end %>
</table>